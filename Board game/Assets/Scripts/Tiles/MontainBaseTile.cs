namespace Tiles
{
    /**
 * Script tile for the classes that are not walkable
 */
    public class MontainBaseTile : BaseTile
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}